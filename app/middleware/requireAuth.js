module.exports=function requireAuth(){return function requireAuthMiddleware(req,res,next){if(req.user)return next();const wantsJson=req.originalUrl.startsWith("/api/")||(req.accepts&&req.accepts("json")==="json");if(wantsJson)return res.status(401).json({success:false,error:"Unauthenticated"});const nextUrl=encodeURIComponent(req.originalUrl);return res.redirect(`/login?next=${nextUrl}`);};};
