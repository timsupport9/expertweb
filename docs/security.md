# Security

Use HTTPS, strong secrets, rate limiting, security headers, input validation, CSRF protection where applicable, authorization checks, secure uploads and least-privilege database credentials.