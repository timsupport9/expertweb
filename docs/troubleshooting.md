# Troubleshooting

Check `storage/logs/`, the `/health` endpoint, environment variables, database connectivity, provider credentials and route registration. Start with `npm start` and inspect the first server error.