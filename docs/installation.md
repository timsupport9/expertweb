# Installation

1. Install Node.js 18+.
2. Run `npm install`.
3. Copy `.env.example` to `.env` and configure it.
4. Ensure the MySQL database exists and matches your database scripts.
5. Run `npm start`.
